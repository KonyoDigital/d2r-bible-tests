# ⏩ RESUME — 2026-09-20 evening

<!-- BEGIN DERIVED — regenerated by tv/resume_state.py; do not hand-edit -->
<!-- fp: head=af122432 origin=af122432 ver=v3430 -->

| | |
|---|---|
| HEAD | `af122432` |
| origin/main | `af122432`  ⏱ *as known 1 min ago — this never fetches* |
| **unpushed** | **0 commit(s)** |
| working tree | **8 file(s) uncommitted** |
| version stamp | v3430 |
| gates registered | 530 |

✅ Nothing is waiting to be pushed — as of the fetch above.

*Derived 2026-09-23 06:56 by `tv/resume_state.py`. Everything outside these markers is narrative a machine cannot measure, written by hand.*

<!-- END DERIVED -->

> Written at his order: *"make sure to make the skills and everything known what to load and how
> to work.. so nothing is lost and everything is starting from the repo by default from where we
> just left off."* `CLAUDE.md` loads automatically and lists the skills; **this file is the state.**

## 0. LOAD FIRST, BEFORE THE FIRST TOOL CALL

`CLAUDE.md` §1 is the authority and it has not changed: **d2r-bible · test-venue ·
regression-guard · the-unjoined-end · unknown-stays-unknown**, always, in this repo. Then the
row that matches the moment the work turns — heart-first for an engine, source-reading-guard for
a guard that greps text, visual-regression-detector for anything rendered, copy-drift when "the
fix did not take", review-after-ship after any push.

⚠ **THE PRIVATE MEMORY IS THE OTHER HALF OF THE BRIEF.** Two files there carry today and must be
read before touching the blocker:
`d2r_session_2026-09-20_evening_state.md` (the resume point, every measurement, every WITHDRAWN
hypothesis) and `d2r_windows_ssh_access.md` (a capability unlocked today — the Windows box is now
fully reachable, backend *and* real screenshots of its native console window). **Those details are
NOT in this file because this repo is PUBLIC.**

## 1. THE ONE ACTION WAITING

**THE VERSIONS ARE BUILT AND PROVEN. The table above says what has and has not shipped — do not
restate it here; a fact written twice goes stale in one of its homes.**

| version | what it does | proofs |
|---|---|---|
| v3397 | `hidden` must actually hide — 7 elements whose `hidden` was defeated by an author `display:` | 3/3 PROVEN |
| v3398 | the window reports the frame it MEASURED, not what it called | 2/2 PROVEN |
| v3399 | the window control he could not find — a bordered button reading `window` | render-checked 375/901/1120/1440 |
| v3400 | `plan()`'s reason REACHES the shelf — the real cause of the 0×0 shelf | 2/2 PROVEN |

508 gates. Second eye looked at v3396 and v3397: clean, **one row each**.

**THE PUSH IS BLOCKED, AND NOT BY THE CODE.** Three refusals:

```
1st  test_control DID NOT FINISH in 1500s — load average 14.66   (he was playing D2R)
2nd  test_control DID NOT FINISH in 1500s — load average  5.85
3rd  test_control HUNG — killed after 1500s on an IDLE machine (load 2.75)
```

⚠ **THE GATE'S VERDICT CHANGES WITH THE LOAD AND IT MISLED ME TWICE.** "STARVED, not hung" is what
it says on a busy machine; only the idle run said HUNG. Do not trust the first refusal's diagnosis.

## 2. THE BLOCKER — what is measured, and the contradiction that is still open

```
cd.run(include_slow=False)          > 8 min, alone, on an idle machine   <- the blocking test's own call
  93 checks timed individually        ~24s of slow ones, total
  /api/board_ownership                 0.12s
  health_engine.report()               2.06s
  json.load(chron_evidence.json)       0.01s
```

**The whole is minutes; every part is seconds.** The decomposition is wrong somewhere and that is
the unfinished thread. Next step: time each check **from inside the real `run()` path** — after
`tick_caches()`, same order, live console — not standalone.

**The one cost actually WATCHED** (task #147): `TestJsSyntaxGate.test_surfaces_parse_in_a_real_js_engine`
sits at **0% CPU** while `js_syntax_gate.check()` launches one headless Chrome per target; the
first was killed at ~90s and **a second spawned for the second target**. Two real defects:
1. `browser_can_load_localhost` probes with `--headless=new` / budget 2000 on a 40-byte page, then
   licenses `--headless=old` / budget 9000 on 5.6 MB `bible.html`. Its own docstring says the
   failure is launch-path specific — and it probes a different launch path.
2. `_LOOPBACK_OK` (js_syntax_gate.py:86) caches the first verdict for the whole process; a real
   90s timeout never invalidates it, so target two pays again.
Intermittency proven: the probe returned **True in 14.0s** (its own timeout is 12s), then minutes
later three variants all timed out at 30s.
⚠ **A heart row predicted this exactly, this morning:** *"it will pass every quiet run and then
hang a push, and the bound will take the blame."* It did, three times.
⛔ Do **not** raise the 90s bound or the 1500s ceiling. The gate is right that a ceiling above
every real load is an absent detector.

## 3. ⚰️ FIVE HYPOTHESES RAISED AND KILLED — do not re-chase them

1. **Console split brain (fresh HTML + fresh label + stale Python).** WITHDRAWN — the console
   re-execs itself, so a PID start time says nothing. It answered `drift.drift=False`,
   `moduleFreshness.stale=False`, `_BOOT_AT` later than the file mtime.
2. **`health_engine.check_vault_receipts` is the hang.** WITHDRAWN — 1.13s. A faulthandler dump is
   one SAMPLE of where a thread was, never proof it sat there.
3. **The three heart rows added today caused it.** WITHDRAWN — the blocking test calls
   `cd.run(include_slow=False)` and `include_periodic` defaults to `include_slow`, so both
   PERIODIC rows are excluded from that path.
4. **`/api/board_ownership` evaluating JS in a hidden window is the cost.** WITHDRAWN — 0.12s.
5. **`frames` is not a real root.** WITHDRAWN — that reading came from a sandbox copy; all five
   roots are `found` on the Mac.

⚠ **STILL TRUE AND UNEXPLAINED:** he reports *"sometimes i need to relaunch the CONSOLE for it to
render properly"* — the symptom is real, my explanation was not. And his Mac console's eagle
reports `rows: 0, "not measured yet"` — **every heart row on the machine he actually uses has
never been asked.**

## 4. STAGED, NOT APPLIED

**v3401** (task #144) — `ABSENT` as a state distinct from `FAILED`, every root declaring who
establishes it, and the heart saying "not here yet, made by X" instead of calling a fresh machine
broken. All 6 anchors verified live, simulated clean on copies, 7-case gate written with 2
red-proofs. ⚠ The scratch dir dies with the job; if the scripts are gone, **task #144 carries
enough to rebuild it**.

## 5. HIS ARCHITECTURE RULING, STANDING

*"make it work as a tree INDIVIDUALLY FOR EACH CONSOLE TO EACH PC... on deans end it should work
in the same form but FROM REMOTE."* The fix ships in code every machine pulls; each console
resolves its own anchors; **nobody fixes a box by hand**. Acceptance is: pull on the other machine,
re-read, confirm. ⛔ Hand-creating a folder to turn a row green is banned.
