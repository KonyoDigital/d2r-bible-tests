name: Routine K — perf watchdog

# Replaces laptop launchd "Routine K" (every 6h). Measures load, boss-overlay
# open, and 2000-trial sim timings (best of 3 runs). Warn-only per spec:
# emits ::warning:: annotations over budget but never fails the build (CI
# runner perf varies — this is a trend signal, not a gate).
on:
  schedule:
    - cron: '30 */6 * * *'
  workflow_dispatch:

  # 2026-08-12 — SWEPT FROM ROUTINE L: this gate audits bible.html and only ever fired on a timer,
  # so a commit that changed the page was not checked until the next day's cron, by which time more
  # ships had landed on top of it. A gate that runs on a clock while the thing it guards changes on
  # pushes is measuring yesterday. Routine L had exactly this and was fixed first; a sweep of the
  # remaining routine-* workflows found four more, of which this is one.
  push:
    paths:
      # v1910 — A GATE MUST SEE THE INPUT IT POLICES, ITSELF INCLUDED. Routine I already says this
      # in its own path list and fixes it for itself; these four did not. Editing K_perf.js (or this
      # workflow) changed what the gate DOES while triggering nothing, so the change landed and the
      # routine that judges it stayed asleep until the next cron — or, if the edit broke it, until
      # a bible.html push blamed someone else's commit.
      - 'bible.html'
      - 'K_perf.js'
      - '.github/workflows/routine-k-perf.yml'
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  perf:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      # 2026-08-19 — A BROKEN UBUNTU MIRROR MUST NOT BE ABLE TO STOP THIS RUN.
      # `playwright install --with-deps` shells out to apt. On this date azure.archive.ubuntu.com
      # answered `Ign` to every index for eight straight minutes; the publish workflow burned its
      # whole 25-minute budget there and surfaced as "cancelled", and routines G and H died the same
      # way on the same push. "Cancelled" reads as "a person stopped this", so the real cause hid.
      # Cache the browser, bound the download, and let the apt top-up fail without taking the job:
      # ubuntu-latest already ships the libraries, and if one is genuinely missing the run below
      # fails on browserType.launch with the library's name in it, which is a far better diagnosis.
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: pw-${{ runner.os }}-
      - name: Install Chromium
        timeout-minutes: 6
        run: npx playwright install chromium
      - name: OS deps for Chromium (best effort)
        timeout-minutes: 5
        continue-on-error: true
        run: npx playwright install-deps chromium
      - name: Run perf watchdog
        run: node K_perf.js "$GITHUB_WORKSPACE/bible.html" | tee k.log
      - name: Check thresholds (warn only)
        run: |
          node -e '
            const fs = require("fs");
            const r = JSON.parse(fs.readFileSync("k.log","utf8").trim().split("\n").pop());
            const lim = { load_ms: 2500, boss_ms: 800, sim_2000_ms: 6000 };
            let warn = false, missing = [];
            for (const k in lim) {
              // v1910 — A MISSING MEASUREMENT IS NOT A PASSING ONE. `undefined > 2500` is false, so
              // a metric K_perf stopped emitting printed "k=undefined (budget 2500ms) OK" and the
              // step signed off with "All perf thresholds green" while measuring nothing. This gate
              // is warn-only about SPEED by choice; it must not be silent about ABSENCE, because
              // "we measured and it was fine" and "nobody measured" are different facts.
              // Measured on the run before this change: load_ms 1997, boss_ms 101, sim_2000_ms 4522.
              if (typeof r[k] !== "number") { missing.push(k); continue; }
              if (r[k] > lim[k]) { console.log(`::warning::K perf ${k}=${r[k]}ms exceeds ${lim[k]}ms budget`); warn = true; }
              else { console.log(`${k}=${r[k]}ms (budget ${lim[k]}ms) OK`); }
            }
            if (missing.length) {
              console.log(`::error::Routine K measured NOTHING for: ${missing.join(", ")} — K_perf.js stopped emitting them, so a green verdict here would mean only that nobody looked`);
              process.exit(1);
            }
            if (!warn) console.log("All perf thresholds green");
          '
