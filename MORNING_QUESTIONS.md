# ☀️ MORNING QUESTIONS — for Konyo (nightly autonomous run 2026-07-23)

Anything I couldn't decide autonomously overnight lands here so I never block the chain. Check `ROADMAP_130.md` for the ✅ progress.

## ❓ Needs your call
1. **G4 Grok — your xAI key + model.** The whole G4 add-on is OFF by default and won't make a single Grok call until you (a) put your xAI key in the env as `XAI_API_KEY` on this Mac, and (b) flip the toggle ON. Default model is `grok-4-latest` (override with `G4_GROK_MODEL`). **Which model does your xAI account have access to?** Nothing fires until you confirm — the OFF path is proven byte-identical to today. When you're ready to try it: set the key, then toggle on (the button, or `TV_G4_GROK=1`, or `touch tv/g4_grok.state`), and the 3 cheap touchpoints go live.
2. **G4 live path is UNTESTED tonight** (by necessity — no key = can't hit xAI). Everything is verified on the OFF path (no-op, zero network, byte-identical). The live agree/disagree logic + the 3 touchpoints are wired but only exercise when you enable it. First real run in the morning is the live smoke test — I'll be around to fix anything the moment you flip it on.

## 🎨 Visual-lock: two design calls (your eye needed — I did NOT auto-do these)
- **Bible letter-spacing / line-height (ls/lh) tokenization — needs YOUR eye, deliberately deferred.** The bible WEIGHT type system is now 100% locked to match the console (0 residual — flagship-look weight-consistency done on both surfaces). But ls/lh is NOT a clean identity fold like weights were: the bible has **87 distinct letter-spacing values + 70 line-height values** (mix of px AND em) vs the console's 7+6 tokens. Folding them means either 157 bible-specific tokens (defeats "one system") or **rounding each to the nearest token = a real VISUAL CHANGE**. Since you care about the exact look, I would NOT auto-round hundreds of spacing values overnight. This wants a dedicated design round with you deciding the target scale (or leaving ls/lh as-is). Say the word on which.
- **`!important` header cleanup** — the ~22 `!important` weight rules are now identity-folded (kept `!important`, look unchanged). Actually *removing* the `!important` is a careful per-rule cascade audit (each was added to win a specific fight; blind removal risks regressions). Worth doing with eyes on each rule, not overnight — flag if you want it.

## 🔒 Visual-lock test — wire it into the gate? (your call)
- A **VISUAL-LOCK invariant test** (`visual_lock_invariant.py`) now enforces the weight lock: it asserts 0 raw `font-weight:NNN` + the `--fw-*` token set, on BOTH bible.html + control_ui.html, and fails loudly with file:line if a raw weight ever creeps back. It already **caught 3 real stragglers** the console pass had missed (folded green). Docs in `LOCKED_TYPE_SYSTEM.md`. I did NOT wire it into the pre-push hook / CI overnight (a red gate mid-run could block the autonomous push chain). **Want it wired into pre-push + Routine-I so drift can't land?** Recommended — say go and I'll add it.

## 💅 Small polish items (your call, non-blocking)
- **Duration format** — the app shows run duration as minutes (e.g. "192m00s") everywhere (shelf/dossier/compare). For multi-hour runs that reads long; an h:m format ("3h12m") would be nicer but it's an app-wide helper change touching every surface. Want it? I'll do it app-wide in one small round.

## ⏸️ Deferred to a careful daytime pass (not autonomous-safe)
- **G3 live-forward auto-route** — the "items auto-route into trackers AS you scan" half of your unified vision. I built the retro back-fill (the 🔄 sweep, ready for your Apply) but held the LIVE-forward wiring for daytime: it touches the live scan pipeline, and I didn't want to risk that autonomously overnight. It's queued — say go when you're up and I'll design it read-first, then wire it gated (same careful pattern as the sweep).

## 🟢 Decisions I made autonomously (veto any in the morning)
- **Shelf card redesign (D18) dropped the inline finds-strip** — the cinematic card now shows the run's HEADLINE grail (gold-lit) + a 🏆-count instead of the full inline item list, which reads cleaner/Maxroll-like. The full "WHAT I FOUND" list is still one tap away in the dossier, and search still indexes every item name. Flagging because you built D1 WHAT-I-FOUND as a flagship — if you want the full inline strip back on the card, say so and it's restored in one round.
- **G4 toggle UI → bible.html, Tools tab, right by the 🔄 Auto-route Sweep panel** (revised from console — cleaner: it's one agent's lane, user-facing, and reads the g4_status/g4_toggle routes directly). OFF by default; shows honest state (OFF / "ON — needs your xAI key" / "ON — active"). It already works headless via env/state file, so this is the convenience switch. Say the word if you'd rather it live in the console instead.
- **G3 sweep:** built exactly to your merge-max / review-gated / chronicle-vs-checker spec; nothing writes to your trackers until you open Tools → 🔄 Auto-route Sweep → Scan → review the diff → Apply. The sunder back-fill (4/6) is waiting there.
- **Visual work brought forward** (type system) ahead of the Phase C feature rounds, because you said the flagship LOOK was your #1 concern. Features resume after the type system is locked.
