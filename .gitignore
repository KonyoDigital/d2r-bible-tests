node_modules/
playwright-report/
test-results/
*.bak
*.bak_*
*.bak.*
.DS_Store

# v42 polish: diagnostic screenshots (mobile audit + viewport tests)
mobile_audit_*.png
mobile_after_*.png
mobile_v42_*.png

# audit JSON regenerates every Routine G fire (every 6h) — noise in diffs
audit_v36_report.json
.autosnap.log
.autosnap.launchd.log

# never commit the bible edit lock
bible.html.EDIT_LOCK
tv/frames/
tv/state.json
tv/known_frames.json
tv/sessions.jsonl

# TV control app runtime
tv/control_agent.log
tv/control_app.log
tv/control_agent.pid
tv/control_capture.pid
tv/sessions.*.jsonl
tv/*.log
tv/.tvd_supervisor_pause
tv/.tvd_window.pid

# G5 Grok Eyes — per-machine mode + shadow log + sub budget (cousin-safe; OFF default)
tv/g5_grok_eyes.state
tv/g5_shadow.jsonl
tv/g5_subscription_budget.json
.wrangler/
tv/.subscription_budget.json

# Point-in-time restore snapshots (large art dumps — keep local, never ship)
backups/RESTORE_*/
backups/RESTORE_*.tar.gz
backups/*.tar.gz

# Windows install stamp (written by install-tvd.ps1 / start_tvd_win.ps1 on this PC only)
tv/.windows_install.json
tv/_*.txt
tv/__pycache__/
