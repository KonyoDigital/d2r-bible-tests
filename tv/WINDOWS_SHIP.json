{
  "platform": "windows",
  "ver": "v3367",
  "product": "TV DIABLO",
  "name": "TV DIABLO Windows",
  "note": "v3367: HEART: v3366 fixed HALF of #115 and I only found out by testing BOTH paths. It taught frozen_frames DEFAULT_DIR to honour TV_GB_SHELF and left the FALLBACK hardcoded, so measured on the shipped tree: with the env set both modules resolved /tmp/fixture and AGREED; with NO env set frozen_frame_watch resolved verify-evidence while frozen_frames resolved gb-shelf, the DEAD folder this whole item is about, still being read by default on the path most runs take. A fix that only covers the overridden case is the unjoined end wearing the fix for the unjoined end. default_dir now resolves at CALL time through the one shared resolver, and the fallback is env-first rather than the bare constant so a machine whose watcher will not import does not quietly go back to the dead folder. ⚠ AND IT RESOLVES AT CALL TIME, NOT IMPORT: DEFAULT_DIR is a module constant evaluated once at load, so an env set afterwards by a test or a caller silently did not take - the scar control_app already carries in its own words, an env honoured only at import is a redirect that silently does not take. VERIFIED LIVE BOTH WAYS after the fix: with no env both resolve verify-evidence and agree True; with the env both resolve the fixture and agree True. TWO NEW CASES: both modules agree WITHOUT an env, and the value resolves at call time rather than at import. ⚠ AND ONE RED-PROOF WENT BLIND AT MATCH COUNT 1 BECAUSE ITS SUBJECT MOVED: the v3366 proof tampered DEFAULT_DIR, which nothing the law checks consults any more, and the string it removed still survives inside default_dir own fallback - an INERT sabotage caused by the code moving underneath it, cause 3 rather than a weak law. Retired deliberately with its reason rather than left passing over nothing, and replaced by a proof that tampers the live path. 485 gates, 10 cases, 4 red-proofs PROVEN.",
  "launcher": "start_tvd_win.ps1",
  "installer": "install-tvd.ps1",
  "capture": "capture_win.ps1",
  "control": "control_app.py",
  "agent": "tv_diablo.py",
  "ui": "control_ui.html",
  "ports": {
    "control": 17772,
    "agent": 17771
  },
  "requires": [
    "tv/start_tvd_win.ps1",
    "tv/control_app.py",
    "tv/control_ui.html",
    "tv/capture_win.ps1",
    "tv/tv_diablo.py",
    "bible.html"
  ],
  "not_used_on_windows": [
    "tv/install-tvd.sh",
    "tv/start_tvd_mac.sh",
    "screencapture",
    "Quartz Screen Recording TCC"
  ]
}
