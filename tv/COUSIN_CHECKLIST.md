# 🪟 COUSIN E2E CHECKLIST (v819) — the one pass that proves the Windows product

Run top-to-bottom on the cousin's machine after `irm https://bull-4-u.com/d2r/install-tvd.ps1 | iex`.
Every step has a truth surface now — nothing should require guessing.

1. **Install** finishes with no red text; Desktop has **TV DIABLO**.
2. Double-click TV DIABLO → the native window opens (if nothing opens: run
   `tv\start_tvd_win.ps1` from a PowerShell to see the loud fail).
3. Footer shows the version (click it → "✓ up to date with origin" — v817 update truth).
4. **Doctor first**: open `http://127.0.0.1:17772/api/doctor` in a browser —
   `"ok": true` required. Any `"severity":"block"` failure prints its own `fix` command.
5. Press **▶ ON AIR** with D2R running:
   - lamp goes mint, film shows THE GAME (capture auto-pins D2R.exe);
   - if it fails, the toast now says exactly why + doctor blockers (v786/v808);
   - 🎯 Capture row shows the pinned window.
6. Hover loot in game → ⚡OCR chip lands fast (v818 twin lane — **first live verify on this box**;
   if no OCR chips ever appear, capture `tv/control_agent.log` and report — vision lane still works).
7. Stash an item → RUN STORY shows 🏦 vault chip; board chronicle ticks.
8. **STOP** → farewell read completes; SIM (Simulation) replays the run as a reel:
   playhead video, T+ clock, forensics strip, ⏪ on the find, credits at the end.
9. 📼 in the theatre → Night Card lands on the Desktop.
10. Report: version footer text + doctor JSON + one Night Card = full state capture.
