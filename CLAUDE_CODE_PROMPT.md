# Claude Code Mission: D2R Bible Autonomous TDD + Sync

You are working on Konyo's D2R Farming Bible — a single-HTML app at `/Users/konyo/Downloads/konyo_d2r_bible_v21.html` (~613KB, vanilla JS, no build step).

## Scope of the project (bounded — finite)
- 11 bosses (countess, andariel, duriel, mephisto, travincal, diablo, baal, pindle, nihl, cows, pit)
- 312 unique items spread across boss drop tables
- 6 difficulty tiers per item × boss (norm, normTz, nm, nmTz, hell, hellTz)
- ~20k cells total — fully deterministic, no streaming, no async, no race conditions
- 6 tabs: bosses, calculator, TZ zones, runes, RotW special, ancients, reference

## Your mission
1. **Run** the existing Playwright suite at `/Users/konyo/d2r_bible_tests/`
2. **Diagnose** any failures by reading both the test expectation and the HTML source
3. **Patch** the HTML file directly to make tests pass (edit `konyo_d2r_bible_v21.html` in place)
4. **Add** missing test coverage for these critical user journeys:
   - Click each of 11 boss chips → Golden Boss Card appears with that boss's data
   - ESC closes the Golden Boss Card
   - TZ zones tab renders ALL 10 zones (Flayer, Crystalline, WSK, Halls, Arcane, RoF, Tristram, Burial, Spider, Catacombs L4)
   - Runes tab renders 5 cards (Countess, Travincal, Hellforge, Hell Bovines, LK Chests)
   - Click item in calculator → aid-card detail panel renders with tabbar at TOP
   - Click any source chip in aid-card → focus updates, 3-stat grid changes
   - Click ⭐ wishlist toggle from inside aid-card header → grail-progress updates
5. **Iterate** until ALL tests pass. Do not stop early.

## What MUST stay invariant (do not regress these)
- The 3 verified silospen anchors:
  - Hell Meph × Shako = 1:912
  - NM Andariel × SoJ = 1:1617
  - Hell Andariel × Bul-Kathos = 1:2721
- ITEMS array has exactly 312 entries
- BOSSES array has exactly 11 entries
- Default MF = 699%, Players = 1
- DIFF_KEYS_ORDERED = ['norm','normTz','nm','nmTz','hell','hellTz']

## Setup (do this first)
```bash
cd /Users/konyo/d2r_bible_tests
# Copy the freshest build for testing
cp /Users/konyo/Downloads/konyo_d2r_bible_v21.html ./bible.html
npm install   # if not done
npx playwright install chromium  # if not done
```

## Run tests
```bash
cd /Users/konyo/d2r_bible_tests
npm test
# or for a single suite:
npx playwright test specs/04_item_routing.spec.ts
```

## Edit workflow
- Read `/Users/konyo/Downloads/konyo_d2r_bible_v21.html`
- Make targeted edits with `Edit` tool — keep changes minimal and surgical
- Always verify `node --check` on the JS portion before saving
- After every batch of edits, re-run `cp` to refresh `bible.html` and re-run Playwright
- Loop until green

## Important conventions
- The HTML is generated by Python build scripts in earlier conversations but Konyo edits the artifact directly now
- Use vanilla JS only — no new dependencies
- All persistence via `localStorage` keys prefixed `d2r_`
- All styles use CSS custom properties (`var(--gold)`, `var(--terror)`, `var(--best)`, etc)
- All template-literal HTML in renderXxx() functions
- Boss IDs are LOWERCASE single words (no spaces)

## What's already implemented in v21
- `renderAidCard(name, opts)` — shared item-detail panel
- `renderBossDetailCard(bossId)` — NEW Golden Boss Card
- `setActiveBoss(id)`, `clearActiveBoss()`
- `setActiveItem(name, bossId, diffKey)`
- 312 ITEM_INFO descriptions (100% coverage)
- 11 BOSS_CHAR_REC + 11 BOSS_ACTION_PLAN entries
- DIFF_MODIFIERS dict with notes + warnings per diff
- 10 static-rendered TZ zone cards
- 5 static-rendered rune farming cards

## Communicate with Konyo via commit messages
After each successful test run, commit with a clear message describing what was fixed.
Konyo is the architect — keep commits atomic and easy to review.

## Stop criteria
- All Playwright tests pass
- New tests added for the 7 critical user journeys listed above
- No regressions to the 3 verified silospen anchors
- `node --check` passes on the embedded script

Good luck. The project is small enough that you should hit all tests green in <10 iterations. Be persistent.
